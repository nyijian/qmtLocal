# 复制这份文件为 .env（同目录下），把下面的值换成你自己的。
# .env 在 .gitignore 里，不会被提交；这份 .env.example 会被提交，
# 只是给换机器/别人看的模板，不含真实值。

# 资金账号。trade_monitor.py / tick_subscribe.py --positions 不传 -a 时用这个。
QMT_ACCOUNT_ID=你的资金账号

# 账号类型：STOCK（默认，不用填也行）/ CREDIT（融资融券）/ FUTURE
QMT_ACCOUNT_TYPE=STOCK
